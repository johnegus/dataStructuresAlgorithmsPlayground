// split a rope so that you return the maximum produce of parts of the rope
let array = [1, 2, 3, 4, 5, 6];
let rope = 5;

const splitRope = (rope) => {
  let max = 0;
  for (let index = 0; index < rope; index++) {
    const element = array[index];
    for (let j = i + 1; j < rope; j++) {
      const element2 = array[j];
      let product;
    }
  }
};

console.log(splitRope(rope));
